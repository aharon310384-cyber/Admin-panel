ВОПРОСЫ ПО ЦЕЛЕВОЙ МОДЕЛИ ДАННЫХ И АДМИНКЕ (Postmanfox)
Составлено: 2026-06-19. Продолжение «вопросы от ИИ.txt».
Цель: прояснить бизнес-модель перед рефакторингом админки и подготовкой к релизу на Timeweb.
Отвечать можно блоками/коротко. Часть вопросов уже частично прояснена в «вопросы от ИИ.txt» — отмечено [уже частично].

═══════════════════════════════════════════════════════════════
A. ЗАКАЗ = (ПОЛУЧАТЕЛЬ + ВИД ДОСТАВКИ)   [уже частично: да, группировка по получателю+доставке]
═══════════════════════════════════════════════════════════════
A1. Подтверждаем: один заказ жёстко = один получатель + один вид доставки? Менять получателя/доставку у существующего заказа нельзя (только новый заказ)?
A2. На заказе нужен и customerId (кто платит), и recipientId (куда везём). Сейчас в коде только customerId. Переделываем на обе связи — верно?
A3. Может ли заказ существовать без получателя на старте (черновик), а получатель назначается позже?
A4. Номер заказа: формат как DT3940PL / OS4057PL — это {код клиента/маршрута}{номер}{страна}? Кто и как генерирует следующий номер?

═══════════════════════════════════════════════════════════════
B. СТРОКИ ЗАКАЗА И ТРЕК-НОМЕРА   [уже частично: строка = товар+кол-во+цена+трек; один трек → много строк]
═══════════════════════════════════════════════════════════════
B1. Строка заказа хранит: трек-номер, наименование (из справочника ProductName или свободный ввод?), количество, цену за единицу, ссылку на товар, объявленную стоимость. Что обязательно, что опционально?
B2. Трек-номер живёт на строке. Нужна ли отдельная сущность «Трек» (чтобы видеть все строки одного трека, статус прихода трека на склад целиком)?
B3. Перемещение строк между заказами — поддерживаем? (клиент перенёс 2 строки из заказа море в заказ авиа) [висел без ответа]
B4. «Неопознанный товар» (пришёл на склад трек, который не привязан ни к одной строке) — это статус строки, отдельная сущность «бесхозный товар», или очередь на складе? [висел без ответа]

═══════════════════════════════════════════════════════════════
C. ПРИЁМКА НА СКЛАД И СТАТУСЫ
═══════════════════════════════════════════════════════════════
C1. Жизненный цикл строки: создана → трек в пути → принята на склад → оформлена в посылку → отправлена → доставлена. Полный список статусов и переходов?
C2. Статус на каком уровне: строка, трек, заказ, посылка? (что меняется при приёмке)
C3. При приёмке менеджер сканирует трек и отмечает что пришло? Частичный приход трека (часть строк пришла) — как отмечается?
C4. Фактический вес заказа измеряется при приёмке (Order.actualWeightKg) — кто и когда вводит? [уже частично: вес на уровне Order после приёмки]

═══════════════════════════════════════════════════════════════
D. ОФОРМЛЕНИЕ НА ОТПРАВКУ / ПОСЫЛКИ
═══════════════════════════════════════════════════════════════
D1. Единица оформления в посылку: весь заказ целиком / выбранные строки (только пришедшие) / по трек-номерам? [висел без ответа]
D2. Консолидация: можно ли склеить 2 разных заказа (одинаковый получатель + доставка) в одну физическую посылку? Кто решает — клиент в ЛК или менеджер? [висел без ответа]
D3. Недоехавшие товары: при закрытии посылки остаток уходит в новый заказ (DT3941PL). Когда создаётся новый заказ — в момент закрытия посылки менеджером или заранее при выборе клиентом? [висел без ответа]
D4. Хранить ли связь новый ↔ родительский заказ (для аудита «отделён от DT3940PL»)? [висел без ответа]
D5. Авто-оформление: тумблер у клиента «собрать в посылку, когда всё принято» — включён по умолчанию? Ждать консолидации или отправлять по мере прихода? [висел без ответа]

═══════════════════════════════════════════════════════════════
E. ВЕСА, РАСЧЁТ СТОИМОСТИ, ТАРИФЫ
═══════════════════════════════════════════════════════════════
E1. Веса (подтверждаем): Order.actualWeightKg, Order.volumetricWeightKg; Parcel.actualWeightKg, Parcel.volumetricWeightKg, Parcel.billableWeightKg (расчётный). Верно?
E2. Объёмный вес: менеджер вводит габариты Д×Ш×В → система считает, или вводит готовый объёмный вес? [висел]
E3. Делитель объёмного веса (5000 авиа / 6000 курьерка) — один на систему или зависит от тарифа/вида доставки? [висел]
E4. Расчётный (billable) вес = max(фактический, объёмный)? Или для моря только фактический, для авиа max? [висел]
E5. Тариф: стоимость = billableWeightKg × ставка_за_кг(направление, вид доставки) + оформление. Источник ставок — наш справочник тарифов (lib/postmanfox-tariffs) или таблица в БД (редактируемая)?
E6. Тарифы сейчас в коде (захардкожены). Нужно ли вынести в БД, чтобы менеджер менял ставки без программиста?

═══════════════════════════════════════════════════════════════
F. ДОП. УСЛУГИ
═══════════════════════════════════════════════════════════════
F1. Уровень применения каждой услуги [висел]:
    - Фото-отчёт — на строку/заказ (сфотографировать конкретные товары)?
    - Страховка — на посылку (% от стоимости)?
    - Упаковка / усиление — на посылку?
    - Консолидация — на посылку?
F2. Услуга добавляется клиентом в ЛК или менеджером в админке (или обоими)?
F3. Цена услуги: фикс / за кг / за лот / % — как в текущем справочнике. Хранить применённые услуги как строки в посылке/заказе?

═══════════════════════════════════════════════════════════════
G. ОПЛАТА И ВАЛЮТЫ
═══════════════════════════════════════════════════════════════
G1. Когда клиент платит [висел]: предоплата при заказе / при оформлении на отправку / постоплата при получении / частями (товар + доставка отдельно)?
G2. За что платит: за товар (китайская цена) отдельно и за доставку отдельно, или одной суммой?
G3. Курс CNY/USD [висел]: менеджер ставит вручную на каждую посылку / из внешнего API с фиксацией / одна глобальная настройка на день?
G4. Все расчёты в кабинете сейчас в USD. На каком этапе фиксируется курс и в какой валюте платит клиент (RUB/USD/CNY)?

═══════════════════════════════════════════════════════════════
H. ТАМОЖНЯ / ДЕКЛАРАЦИЯ
═══════════════════════════════════════════════════════════════
H1. Декларация (категория товара, материал, объявленная стоимость) — на уровне строки, заказа или посылки? [висел]
H2. Кто и когда заполняет — клиент при создании строки или менеджер при оформлении? [висел]
H3. Таможенные лимиты по странам (есть страница на сайте) — нужно ли контролировать в системе (предупреждать о превышении)?

═══════════════════════════════════════════════════════════════
I. ПОЛУЧАТЕЛИ vs КЛИЕНТЫ
═══════════════════════════════════════════════════════════════
I1. Клиент (/clients) — тот, кто заказал и платит, имеет кабинет. Получатель (/recipients) — куда везём (имя+адрес). Верно? [уже частично]
I2. Один клиент → много получателей (себе, жене, родителям)? Получатель может совпадать с клиентом (самовывоз)?
I3. Сейчас получатели хранятся как поля в посылке (recipientName/Address/Phone) + дедуп в /recipients. Нужна ли отдельная таблица Recipient со связью на клиента?

═══════════════════════════════════════════════════════════════
J. КТО И ГДЕ СОЗДАЁТ (АДМИНКА vs КАБИНЕТ)
═══════════════════════════════════════════════════════════════
J1. Заказ создаёт: только клиент в ЛК / только менеджер в админке / оба? [висел — пользователь отклонил вопрос]
J2. Строки заказа добавляет клиент или менеджер?
J3. Оформление на отправку (в посылку) — клиент сам жмёт или только менеджер?
J4. Сейчас в кабинете кнопка «Оформить заказ» отключена (по твоему запросу) — это потому что заказы создаёт менеджер? Подтвердить логику.

═══════════════════════════════════════════════════════════════
K. УВЕДОМЛЕНИЯ
═══════════════════════════════════════════════════════════════
K1. Каналы: Telegram / email / SMS / WhatsApp — какие нужны? [висел]
K2. События: товар пришёл на склад, посылка оформлена, отправлена, доставлена, нужна оплата — какие из них уведомляем?
K3. На старте — откладываем (по твоим словам «пока рано»)? Подтвердить.

═══════════════════════════════════════════════════════════════
L. ПРИОРИТЕТЫ / ЧТО СТРОИМ ПЕРВЫМ
═══════════════════════════════════════════════════════════════
L1. Что доделываем до релиза в первую очередь [висел]:
    - Только админка менеджера (клиент пока пишет в мессенджер)?
    - Админка + минимальный ЛК клиента?
    - Что из ЛК клиента уже считаем готовым (просмотр посылок), а что нет (создание заказа)?
L2. Какие разделы админки «много дел» — приоритизированный список от тебя (Документы? Регистры? Приёмка? Тарифы?).
L3. Рефакторинг модели (Order→получатель, строки, треки, веса) — делаем ДО релиза или релиз на текущей модели, рефакторинг потом?

═══════════════════════════════════════════════════════════════
M. ИНФРАСТРУКТУРА / ДЕПЛОЙ (Timeweb)
═══════════════════════════════════════════════════════════════
M1. Timeweb: обычный VPS (ставим Docker сами) или облачные сервисы Timeweb (managed)?
M2. Домен под кабинет/админку — есть готовый или регистрируем? Поддомен (cabinet.домен / admin.домен)?
M3. БД на старте — SQLite в Docker-томе (просто, но один сервер) или сразу managed PostgreSQL Timeweb (надёжнее, переносимо)? Учесть п.3 (возможна смена сервера).
M4. Отдельный Telegram-бот для кабинета (не тот, через который общаемся) — создаём перед деплоем?
M5. Бэкапы БД на проде — как часто и куда (Timeweb S3 / автоснапшоты)?
M6. Доступ к админке — только сотрудники (роли ADMIN и др.)? Сколько ролей и какие права?

═══════════════════════════════════════════════════════════════
ПОРЯДОК: ответь на блоки A–L (бизнес-логика) — на их основе соберём целевую схему БД
и план рефакторинга. Блок M (деплой) можно ответить отдельно, он не блокирует логику.
═══════════════════════════════════════════════════════════════
