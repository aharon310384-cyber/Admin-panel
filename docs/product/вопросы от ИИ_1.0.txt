8. Что такое «один заказ»?
    - Один трек-номер из одного китайского магазина?
    - Или несколько товаров/треков можно собирать в один заказ (как корзина)?
    - Или это группа товаров, которую клиент сам решил объединить?
  9. Обязательные поля при создании заказа клиентом:
    - Трек-номер (китайский, например СДЭК/SF/YT)?
    - Название товара? (выбор из справочника ProductName или свободный ввод)
    - Количество и цена за единицу?
    - Ссылка на товар у поставщика?
    - Объявленная стоимость для таможни?
    - Фото-отчёт нужен / не нужен?
    - Что-то из этого — необязательное?

  По таможне:

  10. Декларация для таможни — кто и когда заполняет (категория товара, материал, объявленная
  стоимость)? Это на уровне трек-итема, заказа или посылки?

  По оплате и валютам:

  11. Когда клиент платит:
    - Предоплата при создании заказа?
    - При оформлении на отправку?
    - Постоплата при получении?
    - Частями (товар + доставка отдельно)?
  12. Курс CNY/USD — как обновляется?
    - Вручную менеджер ставит на каждую посылку (как сейчас в Parcel.exchangeRateCnyPerUsd)?
    - Берётся из внешнего API при оформлении и фиксируется?
    - Глобальная настройка системы, одна на день?

  По связям с другими сущностями:

  13. Получатель vs Клиент — разница в проекте сейчас неоднозначна. Уточни:
    - Клиент (/clients) — это тот, кто заказал и платит (имеет кабинет)?
    - Получатель (/recipients) — это куда везти посылку (имя + адрес в РФ/Польше/Германии)?
    - Один клиент может иметь много получателей (себе + жене + родителям)?
    - Получатель может быть «самовывозом» — то же лицо, что клиент?
  14. Доп.услуги — на каком уровне применяются:
    - Фото-отчёт — на заказ (сфотографировать конкретные товары)?
    - Страховка — на посылку (% от стоимости всей посылки)?
    - Упаковка — на посылку?
    - Или у каждой услуги свой уровень (см. mock /services — там applicableDeliveryTypes)?

  По автоматизации:

  15. Условие авто-оформления (когда все товары приняты):
    - Этот тумблер у клиента включается по умолчанию или нет?
    - Если у клиента 2 не до конца принятых заказа и один наконец до-приехал — система сама собирает  
  только его в посылку, или ждёт остальные для консолидации?
  16. Уведомления клиенту (email/Telegram/SMS) о приходе товара на склад, оформлении посылки,
  отправке, доставке — нужны? Какие каналы?

  По UI приоритетам:

  17. Что строим в первую очередь (1-2 недели):
    - Только админка для менеджера (клиент пока пишет в WhatsApp)?
    - Сразу и админка, и ЛК клиента (даже минимальный)?
    - Или только ЛК клиента (приёмка/посылки уже работают у менеджера)?

  Отвечай блоками — даже если коротко, типа «8: один трек = один заказ; 9: трек+название+цена
  обязательно, остальное опционально; 10: на трек-итеме при создании; …». Этого хватит, чтобы
  предложить схему Tariff, обновления Order/Parcel и порядок реализации.

✻ Cogitated for 45s

> Самый маленький заказ это одна строка. Но это не один трек номер потому, чтотовары внутри одного    
  трек номера могут быть поделены не несколько строк

● Понял. То есть строка заказа — атомарная единица: товар + количество + цена + трек-номер. Один      
  трек-номер может содержать несколько строк (одна большая поставка → несколько товаров).

  Тогда уточню:

  1. Что объединяет строки в один заказ? По какому признаку клиент/менеджер решает «эти строки — один 
  заказ, а эти — другой»?
    - По дате/магазину?
    - По получателю?
    - Или клиент сам произвольно группирует?
    - Или один заказ = всё, что относится к одному трек-номеру?
  2. Единица оформления на отправку — что именно отправляется в посылку:
    - Только заказ целиком (все его строки)?
    - Можно выбрать отдельные строки из заказа (часть товаров уже на складе → их в посылку, остальные 
  ждут)?
    - Можно выбрать трек-номера (всё, что приехало в этом треке — в посылку)?
  3. Перемещение строк между заказами — можно или нет? Например, клиент создал заказ из 5 строк, потом
  решил 2 из них перенести в другой свой заказ (объединить с третьим заказом)?

  Дай ответы по пунктам — следом задам про вес/тарифы и цикл статусов.

✻ Cogitated for 29s

──────────────────────────────────────────────────────────────────────────────────────────────────────
> User declined to answer questions
  ⎿  · Кто и где создаёт заказ? (Только клиент в личном кабинете / Только менеджер в админке от       
  имени
    History 79/80
──User declined to answer questions
  ⎿  · Кто и где создаёт заказ? (Только клиент в личном кабинете / Только менеджер в админке от       
  имени
  2. Единица оформления на отправку — что именно отправляется в посылку:
    - Только заказ целиком (все его строки)?
    - Можно выбрать отдельные строки из заказа (часть товаров уже на складе → их в посылку, остальные 
  ждут)?
    - Можно выбрать трек-номера (всё, что приехало в этом треке — в посылку)?
  3. Перемещение строк между заказами — можно или нет? Например, клиент создал заказ из 5 строк, потом
  решил 2 из них перенести в другой свой заказ (объединить с третьим заказом)?

  Дай ответы по пунктам — следом задам про вес/тарифы и цикл статусов.

✻ Cogitated for 29s

> по получателю и по виду доставки могут сгрупироваться в один заказ.

● Понял — заказ = (получатель + вид доставки). То есть группировка сделана уже на уровне заказа.      

  Это значит:
  - Если у клиента 2 получателя → минимум 2 заказа
  - Авиа и море = разные заказы, даже если один получатель
  - При оформлении на отправку не нужно повторно выбирать получателя и тариф — они уже на заказе      

  Тогда в схеме на Order должны быть:
  - recipientId (FK на получателя — сейчас вместо этого customerId, надо переделать)
  - deliveryType (уже есть)
  - customerId (отдельно — кто платит и владеет заказом)

  Продолжаю про оформление на отправку:

  2. Единица оформления. Когда клиент жмёт «Оформить на отправку»:
  - (а) уходит весь заказ целиком (все строки) — нельзя оформить часть?
  - (б) клиент может выбрать только те строки, что уже приехали на склад, остальные ждут?
  - (в) или строки группируются автоматически — «принятые на склад» = одна посылка, «в пути» = ждут   
  вторую?

  3. Объединение нескольких заказов в одну посылку. Если у клиента 2 разных заказа, но у них
  одинаковый получатель и одинаковый тип доставки — можно ли их склеить в одну физическую посылку? Кто
  это решает: клиент в ЛК или менеджер на складе?

  4. Перемещение строк между заказами. Например, заказ #1 (получатель А, авиа) и заказ #2 (получатель 
  А, море). Клиент передумал — хочет 2 строки из #2 перенести в #1 (авиа). Это нужно поддержать или   
  нет?





 И жду ещё висящие:
  - 5-bis уровень взвешивания (строка / трек-номер / посылка)
  - Неопознанный — статус или отдельная сущность
  - 10 декларация — где и кто
  - 11 доп.услуги — на каком уровне
  - 12 что строим первым

✻ Churned for 21s

> вес заказа, вес посылки

● Зафиксировал: вес — на уровне Order (после приёмки) и на уровне Parcel (при оформлении), на строке и
  треке веса нет.

  Это значит:
  - Order.weightKg — фактический, после приёмки
  - Parcel.weightKg — фактический, при оформлении (может отличаться из-за упаковки/консолидации)      

  Жду ответы:

  - 7-bis-2 Когда создаётся новый заказ для недоехавших — в момент закрытия посылки менеджером, или   
  заранее при выборе галочек клиентом?
  - 7-bis-3 Номер нового заказа — просто следующий по порядку или дочерний (DT3940PL-2)?
  - Неопознанный — статус строки или отдельная сущность «бесхозный товар»?
  - 10 Декларация для таможни — где и кем заполняется?
  - 11 Доп.услуги (фото/страховка/упаковка) — на уровне строки/заказа/посылки?
  - 12 Что строим первым — админка менеджера, ЛК клиента или оба параллельно?

✻ Sautéed for 16s

> вактический вес заказа, объемный вес заказа, фактический вес посылки, объемный вес посылки,         
  расчетный вес посылки

● Понял, расширил веса:

  ┌────────────────────┬─────────┬──────────────────────────────────────────────────────┐
  │        Поле        │ Уровень │                         Что                          │
  ├────────────────────┼─────────┼──────────────────────────────────────────────────────┤
  │ actualWeightKg     │ Order   │ Фактический вес заказа (весы при приёмке)            │
  ├────────────────────┼─────────┼──────────────────────────────────────────────────────┤
  │ volumetricWeightKg │ Order   │ Объёмный вес заказа (по габаритам)                   │
  ├────────────────────┼─────────┼──────────────────────────────────────────────────────┤
  │ actualWeightKg     │ Parcel  │ Фактический вес посылки (после упаковки)             │
  ├────────────────────┼─────────┼──────────────────────────────────────────────────────┤
  │ volumetricWeightKg │ Parcel  │ Объёмный вес посылки (по габаритам)                  │
  ├────────────────────┼─────────┼──────────────────────────────────────────────────────┤
  │ billableWeightKg   │ Parcel  │ Расчётный вес — по нему считается стоимость доставки │
  └────────────────────┴─────────┴──────────────────────────────────────────────────────┘

  Уточнения по весам:

  5-ter. Откуда объёмный вес:
  - Менеджер вводит габариты (Д×Ш×В) → система считает (Д×Ш×В / делитель)
  - Или менеджер вводит уже готовый объёмный вес?

  5-quad. Делитель объёмного веса (обычно 5000 для авиа, 6000 для курьерки):
  - Один на всю систему?
  - Зависит от тарифа (привязан к типу доставки)?

  5-cinq. Формула расчётного веса:
  - max(фактический, объёмный) — стандарт
  - Или другая (для авиа max, для моря только фактический)?

  И всё ещё висят (важные для следующего шага):
  - 7-bis-2 Когда создаётся новый заказ для недоехавших
  - 7-bis-3 Нумерация нового заказа
  - Неопознанный — статус или сущность
  - 10 Декларация для таможни
  - 11 Уровень доп.услуг
  - 12 Что строим первым

✻ Brewed for 23s

> DT3941PL

● Зафиксировал: номер нового заказа = следующий по порядку (DT3941PL), без пометки «дочерний».        

  Мини-уточнение: хранить ли связь новый ↔ родительский заказ? Чтобы в карточке DT3941PL было видно   
  «отделён от DT3940PL», а в карточке DT3940PL — «часть строк ушла в DT3941PL». Полезно для аудита и  
  истории, или забить?

  И жду:
  - 7-bis-2 Когда создаётся новый заказ — в момент закрытия посылки менеджером или заранее при выборе 
  галочек клиентом?
  - Неопознанный — статус строки или отдельная сущность?
  - 10 Декларация для таможни — где и кем?
  - 11 Доп.услуги — на каком уровне?
  - 12 Что строим первым?










Второе интерьвю

Что в папке и связь с админкой
Файл	Что это	Связь с админкой
вопросы от ИИ.txt	Интервью про целевую бизнес-модель (заказы/посылки/веса/тарифы)	🔴 Ключевое — задаёт, какой админка должна стать
book-2.xlsx + book-2-import.json	Исходные данные: 85 клиентов + посылки из Excel	Это реальные данные, импортированные в админку (Customer, Parcel). Поля sourceSheet/sourceRow в БД — следы этого импорта
Наименования товаров.xlsx	Справочник наименований	→ таблица ProductName (раздел «Наименования»)
OS4057PL.jpg, …png, photo….jpg	Картинки (вероятно скрины исходных таблиц/фото)	Вспомогательное
%% Основной управляющий блок.docx	❌ Это про другой проект (онлайн-школа, многоагентная система)	Случайно лежит, к админке не относится
Главный вывод (то, что надо обсудить)
Есть расхождение между текущей моделью данных и целевой (из «вопросы от ИИ.txt»):

Сейчас в коде:

Order = товары (таблица Product), Parcel = посылки, привязка к customerId.
Целевая модель из интервью:

Строка заказа = товар + кол-во + цена + трек-номер (атомарная единица; один трек → много строк).
Заказ = (получатель + вид доставки) — т.е. Order должен ссылаться на получателя (recipientId), а не только на клиента.
Веса на двух уровнях: заказ (фактич./объёмный) и посылка (фактич./объёмный/расчётный — по нему считается доставка).
Вот почему «по админке много дел» и «нужен рефакторинг» — текущая структура не соответствует тому, как реально устроен бизнес (треки, группировка по получателю+доставке, веса, расчёт стоимости).

Открытые вопросы из интервью (остались без ответа)
Декларация для таможни (кто/где), уровень доп.услуг (строка/заказ/посылка), авто-оформление, «неопознанный товар» (статус или сущность), что строим первым.